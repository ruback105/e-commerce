import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  toolBar: {
    justifyContent: 'space-between',
  },
  title: {
    display: 'flex',
    alignItems: 'center',
    textDecoration: 'none',
  },
}))
